export default class Disciplina {
	constructor({id, nome, ementa, id_curso, status}) {
			this.id = id;
			this.nome = nome;
			this.ementa = ementa;
			this.id_curso = id_curso;
			this.status = status;
	}
}
